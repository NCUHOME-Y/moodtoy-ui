import { Avatar, Container, Typography } from '@mui/material'

const Me = () => {
  return (
    <Container maxWidth="xs">
      <Avatar sx={{ width: 8 * 14, height: 8 * 14 }}>H</Avatar>
      <Typography variant="h4">WWW</Typography>
    </Container>
  )
}

export default Me
